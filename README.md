# 🚀 Serverless Terminal CV

A smart, high-performance CLI resume built on serverless edge architecture. It dynamically detects the user's incoming platform environment via HTTP request headers to serve a custom experience.

## 🌟 Live Demo

To test this live from your own machine, open up a terminal and run:

```bash
curl [https://cv.dulithdivisekara.workers.dev](https://cv.dulithdivisekara.workers.dev)
